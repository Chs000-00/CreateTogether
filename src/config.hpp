#pragma once

#define MOD_LOBBY_NAME "CreateTogether"
#define MOD_VERSION "v0.2.0"

namespace constants {
    const int LARGEST_SPEED = 4; // Right now this is Speed::Fastest
    const int LARGEST_SELECT_TYPE = 1; // SelectArtType::Ground
}