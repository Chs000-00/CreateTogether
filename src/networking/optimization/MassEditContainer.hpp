#pragma once

// Certain actions, such as edit group ID's, are only able to be hooked in such a way, that it would
// require calling the send function once per object. SteamWorks should in theory already do this, on second thought. Needs more research.
struct MassEdits {
    
}